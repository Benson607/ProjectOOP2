main.cpp
	map.setEvent();
	GameLoop大改
	添加void show_player_equipment_buff(std::vector<Entity*>& roles);
	void show_player_equipment_buff(std::vector<Entity*>& roles) { ... }

map.cpp
	getinput新增Entity& entity
	void Map::getinput(Entity& entity,int player, int player1_x, ...){ ... }
	
	新增std::vector<std::string> map_ui_again{ ... }
	void show_map_ui_again(){ ... }
	(在最上面)	

	!!你老婆有改pick_inventory(){ ... }!!

map.h
	getinput新增Entity& entity
	void Map::getinput(Entity& entity,int player, int player1_x, ...);

rect.cpp
	Rect(int type){ ... }新增 else if(type==7){ ... }

entity.cpp
	Entity::Entity(int type, std::string name) :Stat(), name(name){ ... }內的Buff預設

